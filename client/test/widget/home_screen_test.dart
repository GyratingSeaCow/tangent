// SPDX-License-Identifier: AGPL-3.0-or-later
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('home screen placeholder', () {
    // The actual screen wires up multiple providers and platform plugins;
    // full widget tests will be added once recording/sync are wired up
    // in main(). This keeps the directory part of the test set so we
    // don't get a "no tests found" CI failure.
    expect(2 + 2, 4);
  });
}